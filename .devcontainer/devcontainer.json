{
  "name": "Ubuntu",
  "build": {
    "dockerfile": "Dockerfile",
    "args": {
      "VARIANT": "ubuntu-22.04"
    }
  },
  // "forwardPorts": [4000],
  // "postCreateCommand": "cabal build",
  "settings": {
    "terminal.integrated.defaultProfile.linux": "zsh" // set your favorite shell
  },
  "extensions": ["haskell.haskell"],
  "remoteUser": "vscode"
}
