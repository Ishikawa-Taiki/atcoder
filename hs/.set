# 必要なセットアップ処理(あれば)

# テンプレート切り替え
acc config default-template hs

# エイリアス設定(提供コマンドは全言語共通とする)
alias contest='acc new'
alias run='runghc Main.hs'
alias test='ATCODER_LAST_TEST_TIME=`date +%Y%m%d-%H%M%S`;oj test -c "runghc Main.hs" -N > $ATCODER_LAST_TEST_TIME-hs.log;code ./$ATCODER_LAST_TEST_TIME-hs.log'
alias next='acc add'
alias submit='acc submit -s -- -y'
alias edit='code ./Main.hs'
